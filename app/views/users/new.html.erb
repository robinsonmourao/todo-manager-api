<div class="container-user-form-screen">
  <div class="left-side-signup"></div>
  <div class="right-side-signup">
    <%= form_with(model: @user, url: signup_path) do |form| %>
      <div class="form-user-textboxes">
        <%= form.label :username %>
        <%= form.text_field :username, placeholder: "Nome" %>

        <%= form.label :email %>
        <%= form.text_field :email, placeholder: "Email" %>

        <%= form.label :password %>
        <%= form.password_field :password, placeholder: "Senha" %>

        <%= form.submit "Signup", id: "user-signup-button" %>
      </div>
    <% end %>
  </div>
</div>
